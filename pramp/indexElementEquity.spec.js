const { indexElementEquity } = require('./indexElementEquity');

it('should test indexElementEquity', () => {
  // expect(indexElementEquity([-8,0,2,5,6,7,8,9,10])).toBe(2);
 })
 it('should test indexElementEquity', () => {
  // expect(indexElementEquity([-1,0,3,6])).toBe(-1);
 })
